1.函数一：
	创建一个以最喜欢的作者为值的数组
	并且把这个数组传递给方法lineSearch
	然后根据返回结果打印查找值的位置，如果找不到就打印找不到
2.函数二（lineSearch）：
	根据传输进来的关键字查找数组
	如果找得到就返回位置，找不到就返回-1


	先定义一个数组，从键盘读取值，并存入一个数组
	随后调用lineSearch函数
	并用一个int 变量res存好函数的执行结果
	根据res打印值

	根据字符串查找数组，并返回键值的函数